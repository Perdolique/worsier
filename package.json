{
  "name": "worsier-workspace",
  "version": "3.0.1",
  "private": true,
  "packageManager": "pnpm@11.22.0",
  "scripts": {
    "benchmark": "pnpm --filter worsier build && pnpm --filter worsier build:native && pnpm --filter worsier-benchmark benchmark",
    "benchmark:update": "pnpm --filter worsier build && pnpm --filter worsier build:native && pnpm --filter worsier-benchmark benchmark:update",
    "benchmark:verify": "pnpm --filter worsier-benchmark benchmark:verify",
    "build": "pnpm --filter worsier build",
    "check": "pnpm check:versions && cargo test --workspace && pnpm --filter worsier-benchmark test && pnpm --filter worsier build && pnpm check:types && pnpm --filter worsier build:native && pnpm --filter worsier test && pnpm --filter worsier test:packed && pnpm --filter worsier-benchmark smoke && pnpm --filter worsier-benchmark benchmark:verify",
    "check:types": "tsc -p tsconfig.tools.json",
    "check:versions": "node scripts/check-versions.mts",
    "generate": "cargo run -p worsier-formatter --example generate_schema && node scripts/generate-types.mts"
  },
  "devDependencies": {
    "@napi-rs/cli": "3.8.6",
    "@types/node": "26.2.0",
    "json-schema-to-typescript": "15.0.4",
    "typescript": "7.0.2"
  }
}

{
  "name": "worsier-benchmark",
  "private": true,
  "type": "module",
  "scripts": {
    "benchmark": "node src/cli.mjs run",
    "benchmark:update": "node src/cli.mjs update",
    "benchmark:verify": "node src/cli.mjs verify",
    "smoke": "node src/cli.mjs smoke",
    "test": "node --test test/*.test.mjs"
  },
  "devDependencies": {
    "oxfmt": "0.63.0",
    "prettier": "3.9.6",
    "worsier": "workspace:*"
  }
}
